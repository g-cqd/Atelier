import Foundation

/// Exposes the KittySyntax module bundle for resource access.
/// Tests use `@testable import KittySyntax` to reach this.
enum KittySyntaxResources {
    static let bundle: Bundle = .module
}
